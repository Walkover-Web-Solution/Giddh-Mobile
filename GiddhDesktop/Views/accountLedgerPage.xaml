<Page
    x:Class="GiddhDesktop.Views.accountLedgerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GiddhDesktop.Views"
    xmlns:converterss="using:GiddhDesktop.Common.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <converterss:convertStringToDate x:Key="stringToDate"/>
        <converterss:showNameWithUniqueName x:Key="getFullName"/>
        <converterss:sharedEntityToBool x:Key="sharedEntityToBool"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="mainGrid" DataContext="{Binding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--#region date section to get ledger-->
            <StackPanel Orientation="Horizontal" Grid.Row="0" BorderThickness="0" BorderBrush="Transparent" Margin="0,0,0,10">
                <CalendarDatePicker x:Name="fromDatePicker" DateFormat="{}{day.integer}/{month.integer}/{year.full}" BorderThickness="0,0,0,1"/>
                <TextBlock Text="to" VerticalAlignment="Center" Margin="5,0"/>
                <CalendarDatePicker x:Name="toDatePicker" DateFormat="{}{day.integer}/{month.integer}/{year.full}" BorderThickness="0,0,0,1"/>
                <Button Content="Go" Margin="5,0,0,0" Click="goButton_Click"/>
            </StackPanel>
            <!--#endregion-->

            <!--#region Create entry from here-->
            <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10" Padding="10" BorderThickness="2" BorderBrush="LightGray">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!--<TextBlock Text="Make Entry" Grid.Row="0" Foreground="Gray" FontSize="11" Margin="0,5"/>-->
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
                    <CalendarDatePicker BorderThickness="0" DateFormat="{}{day.integer}/{month.integer}/{year.full}" IsHitTestVisible="True"/>
                    <AutoSuggestBox VerticalAlignment="Center" PlaceholderText="Particular" Width="300" BorderThickness="0" TextChanged="AutoSuggestBox_TextChanged" SuggestionChosen="AutoSuggestBox_SuggestionChosen">
                        <AutoSuggestBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource getFullName}}"/>
                            </DataTemplate>
                        </AutoSuggestBox.ItemTemplate>
                    </AutoSuggestBox>
                    <TextBox VerticalAlignment="Center" PlaceholderText="Amount" Width="150" TextAlignment="Right" BorderThickness="0" InputScope="CurrencyAmount"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
                    <CalendarDatePicker BorderThickness="0" DateFormat="{}{day.integer}/{month.integer}/{year.full}" IsHitTestVisible="True"/>
                    <AutoSuggestBox VerticalAlignment="Center" PlaceholderText="Particular" Width="300" BorderThickness="0" TextChanged="AutoSuggestBox_TextChanged" SuggestionChosen="AutoSuggestBox_SuggestionChosen">
                        <AutoSuggestBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource getFullName}}"/>
                            </DataTemplate>
                        </AutoSuggestBox.ItemTemplate>
                    </AutoSuggestBox>
                    <TextBox VerticalAlignment="Center" PlaceholderText="Amount" Width="150" TextAlignment="Right" BorderThickness="0" InputScope="CurrencyAmount"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Margin="0,10" Grid.ColumnSpan="2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="voucherTypeCombo" Grid.Column="0" PlaceholderText="Select voucher type"/>
                        <RichEditBox x:Name="descriptionBox" Grid.Column="1" PlaceholderText="Description"/>
                        <TextBox x:Name="tagTextBox" Grid.Column="2" PlaceholderText="Tag"/>
                    </Grid>
                </StackPanel>
                <StackPanel Grid.Row="3" Orientation="Horizontal">
                    <Button Content="ADD" Margin="0,0,10,0" Background="{x:Null}">
                        <Button.BorderBrush>
                            <SolidColorBrush Color="#CCE6E6E6"/>
                        </Button.BorderBrush>
                    </Button>
                    <Button Content="CLEAR" Background="{x:Null}" BorderBrush="#CCE6E6E6"/>
                </StackPanel>
            </Grid>

            <!--#endregion-->

            <!--#region gridview header-->
            <StackPanel Orientation="Vertical" Grid.Row="2" Grid.Column="0">
                <Border Background="#FF4D3F3F" Padding="10">
                    <TextBlock Text="Debit (Dr)" Foreground="White" FontWeight="Bold"/>
                </Border>
                <Grid Background="LightGray" Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Date" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Particular" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Amount" FontWeight="SemiBold"/>
                </Grid>
            </StackPanel>
            <StackPanel Orientation="Vertical" Grid.Row="2" Grid.Column="1">
                <Border Background="#FF4D3F3F" Padding="10">
                    <TextBlock Text="Credit (Cr)" Foreground="White" FontWeight="Bold"/>
                </Border>
                <Grid Background="LightGray" Padding="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Date" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Particular" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Amount" FontWeight="SemiBold"/>
                </Grid>
            </StackPanel>
            <!--#endregion-->

            <!--#region debit transactions-->
            <GridView ItemsSource="{Binding debitTransactions}" Grid.Row="3" Grid.Column="0" BorderThickness="0,0,1,0" Padding="5" SelectionMode="Single" BorderBrush="#FFBDBDBD">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"  MaximumRowsOrColumns="1"></ItemsWrapGrid>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" DataContext="{Binding}">
                            <CalendarDatePicker Date="{Binding entryDate,Converter={StaticResource stringToDate}}" BorderThickness="0" DateFormat="{}{day.integer}/{month.integer}/{year.full}" IsHitTestVisible="True"/>
                            <TextBox VerticalAlignment="Center" Text="{Binding particular.name}" Width="300" BorderThickness="0"/>
                            <TextBox VerticalAlignment="Center" Text="{Binding amount}" Width="150" TextAlignment="Right" BorderThickness="0"/>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <!--#endregion-->

            <!--#region credit transactions-->
            <GridView ItemsSource="{Binding creditTransactions}" Grid.Row="3" Grid.Column="1" Padding="5">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"  MaximumRowsOrColumns="1"></ItemsWrapGrid>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" DataContext="{Binding}">
                            <CalendarDatePicker Date="{Binding entryDate,Converter={StaticResource stringToDate}}" BorderThickness="0" DateFormat="{}{day.integer}/{month.integer}/{year.full}"/>
                            <TextBox VerticalAlignment="Center" Text="{Binding particular.name}" Width="300" BorderThickness="0"/>
                            <TextBox VerticalAlignment="Center" Text="{Binding amount}" Width="150" TextAlignment="Right" BorderThickness="0"/>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <!--#endregion-->

        </Grid>
    </Grid>
</Page>
